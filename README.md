# Override picture in picture
Overrides flags for picture in picture mode so googles picture in picture mode always works

# How to install a local extension
1. git clone this repo to some ```<path>```
2. visit chrome://extensions > Enable Developer Mode (top right corner)
3. chrome://extensions > Load Unpacked (top left corner) > select the cloned folder ```<path>``` from 1.


